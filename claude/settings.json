{
  "enabledPlugins": {
    "rust-analyzer-lsp@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if [[ \"$CLAUDE_FILE\" == *.rs ]]; then cargo check --quiet 2>&1 | head -20; elif [[ \"$CLAUDE_FILE\" == *.py ]]; then mypy \"$CLAUDE_FILE\" --no-error-summary 2>&1 | head -10; fi'"
          }
        ]
      }
    ]
  }
}
